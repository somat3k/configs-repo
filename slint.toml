# Slint project configuration
# See: https://slint.dev/releases/latest/docs/slint/

[include-paths]
# Additional include paths for .slint files
ui = "src/ui"

[compiler]
# style = "material"   # material | fluent | cupertino
# backend = "winit"    # winit | qt | software-renderer
